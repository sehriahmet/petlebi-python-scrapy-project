CREATE TABLE petlebi (
    id INT AUTO_INCREMENT PRIMARY KEY,
    url VARCHAR(255),
    name VARCHAR(255),
    barcode VARCHAR(255),
    price VARCHAR(255),
    stock VARCHAR(255),
    images TEXT,
    description TEXT,
    category VARCHAR(255),
    brand VARCHAR(255)
);
